# agent-examples

This repo collects several examples of community agents and MCP tools written in different
frameworks and exposed with agent-to-agent protocols and tools protocols such as A2A and MCP.

```
# agent-examples

This repo collects several examples of community agents and MCP tools written in different
frameworks and exposed with agent-to-agent protocols and tools protocols such as A2A and MCP.

## Environment Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `UV_CACHE_DIR` | `/app/.cache/uv` | Cache directory for `uv`. Required by most examples. Set this if your environment does not provide it automatically (e.g. Kagenti sets it for you). |
```
